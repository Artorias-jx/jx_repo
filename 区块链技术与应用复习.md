📚 战略二：核心考点精准爆破（背诵与理解指南）

根据你的期末总结文档，以下是必须掌握的满分要点：

考点 1：区块链概述 & 应用 (简答/填空)

- **区块链定义**：由顺序连接的区块构成的分布式账本，在多个参与者之间共享，并通过共识机制进行更新。
- **5个应用场景**（任记5个）：加密货币、智能合约、金融服务、供应链管理、身份验证与隐私、医疗健康、版权保护、选举与投票。

考点 2：密码学基础 (填空/简答)

- **哈希函数是什么**：一种将**任意长度**的输入映射为**固定长度**输出（比特币为256位）的数学函数。
- **哈希函数三大特征**（必考）：
    1. **抗碰撞性**：极难找到两个产生相同哈希值的不同输入，用于防止数据篡改。
    2. **隐藏性**：无法从哈希值反推原始输入，用于数字承诺机制。
    3. **谜题友好性**：只能靠暴力穷举寻找满足条件的哈希值，为PoW工作量证明提供基础。

考点 3：身份生成路径 (简答题重点)

- **私钥是什么**：本质上是一个随机生成的大整数（256位），代表你对资产的绝对控制权。
- **私钥** → **公钥**：利用**椭圆曲线密码学（secp256k1）** 进行点乘运算（跳跃机制）。这是一个“单向加密函数”，正向计算（私钥乘基点G得到公钥）容易，但反向推导（从公钥推私钥）极其困难。 
- **公钥** → **地址**：将公钥先后经过 **SHA-256** 和 **RIPEMD-160** 两次哈希运算得到“公钥哈希”（20字节）；然后在前面加上**版本号**，末尾加上**校验码**（凑成25字节）；最后将整体进行 **Base58Check 编码**，生成最终的比特币地址。

考点 4：交易与 UTXO (简答题重点)

- **UTXO概念**：未花费的交易输出。本次交易的输入是过去交易的输出，本次的输出是未来交易的输入。
- **输入 (Inputs) 的关键字段**：
    - `txid` 和 `vout`：引用上一笔交易的某个输出，指明钱从哪来。
    - `ScriptSig`（解锁脚本）：包含发送人的**数字签名和公钥**，用于证明自己有权花这笔钱。
- **输出 (Outputs) 的关键字段**：
    - `Value`：转账金额。
    - `scriptPubKey`（锁定脚本）：包含接收者的公钥哈希，是加密谜题，设定了解锁这笔资金的条件。

考点 5：数据结构与安全性 (简答题重点)

- **区块头结构**：包含6个字段：版本号、父区块哈希值（Prevhash）、时间戳、难度目标（Bits）、随机数（Nonce）、**Merkle 根**。
- **区块体结构**：区块体是区块的主体部分，**占用了绝大多数存储空间（平均大于 99%）**。它主要由两部分构成：首先记录**交易数量**，随后依次记录已验证的**每笔交易的完整结构**（包括版本号、输入列表、输出列表和锁定时间等）。**易错点注意：区块体中不存储 Merkle 树结构本身**，Merkle 树只是用于计算的临时工具，计算完成后只有 Merkle Root 会被永久写入区块头
- **Merkle树的作用**：将区块内所有交易逐层哈希生成一个Root。它使得轻节点（SPV）在不下载完整区块的情况下，只需极少数据（不到1KB的Merkle路径）就能高效验证某笔交易是否上链。
- **数据结构如何保证安全**：
    1. **链式连接**：通过区块头包含“上一个区块哈希”，形成防篡改链条。改动任何历史数据都会导致后续所有哈希失效，极大增加攻击成本。
    2. **Merkle树**：保护区块体内交易的完整性，防止单笔交易被篡改。

考点 6：铸币机制与对比 (简答/填空)

- **铸币细节**：初始奖励50BTC，每210,000个区块（约4年）减半一次，总量固定在2100万枚。
- **法币 vs 比特币 5个维度对比**（必背表格）：
    1. _发行权_：政府央行控制 vs 算法规则预设（无人为干预）。
    2. _总量上限_：无上限/可增发 vs 固定上限2100万。
    3. _调节机制_：人为加息降息印钞 vs 仅通过算法每21万区块减半。
    4. _通胀/通缩_：通胀为主（货币贬值） vs 通缩趋势（稀缺性递增）。
    5. _储蓄者/债务者_：通胀惩罚储蓄者/利好债务人 vs 通缩保护储蓄者/不利债务人。

---

🏆 战略三：拿下30分综合题的终极答题模板

明确了是考 **共识的四个过程+共识安全（分叉及链级共识）。 

**1. 共识的四个独立过程（按顺序写，指出是谁在验证）：**

- **过程一：验证交易是否有效**。完整节点接收到用户发送的新交易后，验证其数字签名是否合法、引用的UTXO是否存在且未被双花。
- **过程二：打包交易并进行PoW挖矿**。矿工节点将验证通过的交易打包进候选区块，并不断改变区块头中的Nonce（随机数），进行SHA-256哈希计算，直到计算出的区块头哈希值小于当前网络规定的目标值（Target），完成工作量证明。
- **过程三：验证新区块是否合法**。当矿工成功挖出区块并广播后，网络中的其他完整节点会独立验证该区块的合法性（包括PoW难度是否达标、区块内的交易是否全都有效）。
- **过程四：组装与选择区块链（链级共识）**。节点将验证通过的新区块连接到区块链上。当出现分叉时，所有节点遵循“最长链（最大累计工作量）规则”进行选择。

**2. 分叉是如何来的？如何解决？（链级共识）：**

- **产生原因**：当两个矿工几乎同时挖出合法的新区块，且都指向同一个父区块时，由于网络传播延迟，不同节点会先后收到不同的区块，临时选择不同的链尖，导致网络短暂分裂。
- **解决机制（重新收敛）**：矿工基于各自认为的主链继续挖矿。当某条分叉链率先挖出下一个新区块，形成了**更大的累计工作量（最长链）**时，全网节点会重新比较，统一切换到累计工作量最大的这条链上。原分叉链（孤块）被抛弃，网络重新达成统一共识。

**3. 共识安全（51%攻击）：**

- **本质**：前提是多数算力诚实。但如果攻击者掌握全网超过51%的算力，就可以利用“最长链规则”暗中构造一条更长的替代链来取代当前主链。
- **能做什么/不能做什么**：攻击者只能实施**双花攻击（撤销自己刚发出的付款）**、或者拒绝打包某人交易。但**无法凭空伪造比特币，也无法在没有私钥的情况下窃取他人的比特币**，因为这受限于密码学而非算力。

