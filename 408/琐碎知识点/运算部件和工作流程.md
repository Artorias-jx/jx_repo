# 运算部件结构与工作流程

## 1. 加减运算部件（ALU）
![加减运算部件](assets/运算部件和工作流程/file-20260728215215405.png)
**基本结构**：
```
X (n位) ──→ ┐
            ├──→ n位加法器 ──→ F (n位结果)
Y (n位) ──→ MUX ──→ Y' (n位)     ↓
  ↑         ↑                  ZF, SF, CF, OF
  └── 求反 ──┘
     Sub信号控制
```

**工作流程**：
1. **加法**：Sub=0，Y直接进入加法器，F = X + Y
2. **减法**：Sub=1，Y取反+1（补码），F = X + (-Y) = X - Y
3. **标志位生成**：
   - ZF：结果全0检测
   - SF：结果最高位
   - CF：最高位进位⊕Sub（Cin）
   - OF：符号位进位 ⊕ 最高数值位进位

**关键点**：减法通过"加补码"实现，只需一个加法器

---

## 2. 无符号数乘法器（右移型）
![](assets/运算部件和工作流程/file-20260728215238321.png)
**基本结构**：
```
被乘数X(32位) → 32位ALU → 64位结果
                    ↓
乘数Y(32位) ← 右移 ← 乘积寄存器P(32位) | 乘数寄存器Y(32位)
                            ↑
                    控制逻辑(Cn) ← 时钟
```

**工作流程**：
1. 初始化：P=0，Y=乘数
2. 循环n次：
   - 若Y最低位=1：P = P + X
   - 若Y最低位=0：P不变
   - PY整体右移1位（P的最低位进入Y的最高位）
3. 结果：64位 = P(高32位) + Y(低32位)

**特点**：部分积逐步累加，每次右移让乘数的下一位参与运算

---

## 3. 有符号数乘法器（补码一位乘法-Booth算法）
![](assets/运算部件和工作流程/file-20260728215243121.png)
**基本结构**：
```
被乘数X(32位) → 32位ALU(加/减) → 64位结果
                    ↓
乘积寄存器P(32位) | 乘数寄存器Y(32位)
        ↓ 右移（算术右移）
    控制逻辑 ← Y的最低位 + 辅助位
```

**工作流程**（简化版Booth）：
1. 初始化：P=0，Y=乘数，Y_{-1}=0（辅助位）
2. 循环n次，根据(Y_0, Y_{-1})决定操作：
   - (1,0)：P = P - X（减被乘数）
   - (0,1)：P = P + X（加被乘数）
   - (0,0)或(1,1)：P不变
   - PY整体算术右移1位
3. 结果：64位补码

**优势**：可以处理负数，减少部分积加法次数

---

## 4. 除法器
![](assets/运算部件和工作流程/file-20260728215246834.png)
**基本结构**：
```
除数Y(32位) → 32位ALU(加/减) → 余数
                ↓
余数寄存器R(32位) | 商寄存器Q(32位)
        ← 左移
    控制逻辑Cn ← 时钟
```

**工作流程（不恢复余数法）**：
1. 初始化：R=0，Q=被除数
2. 循环n次：
   - RQ整体左移1位
   - 若R≥0：R = R - Y，商位=1（够减）
   - 若R<0：R = R + Y，商位=0（不够减）
   - 商位送入Q的最低位
3. 若最后R<0，执行R = R + Y恢复余数
4. 结果：Q=商，R=余数

**异常检测**：
- **除数为0**：硬件检测Y=0，触发异常
- **溢出**：被除数绝对值≥除数绝对值（有符号数），设置OF=1

 一、 课件核心错误对照表

| 错误类别                 | 课件错误表述                                        | 408 正确理论规范                                                                                       | 错误本质与危害                                         |
| :------------------- | :-------------------------------------------- | :----------------------------------------------------------------------------------------------- | :---------------------------------------------- |
| **错误 1：寄存器 $R$ 初始化** | `R = 0, Q = 被除数`                              | **无符号数**：$R$ 进行**零扩展**（设为 $0$）<br>**有符号补码**：$R$ 必须进行**符号扩展**（正数填 $0$，负数填 $1$）                    | 遗漏有符号负数的扩展逻辑，直接写死 $R=0$ 会导致负被除数计算彻底错误           |
| **错误 2：加减交替判定规则**    | `若 R >= 0: R = R - Y`<br>`若 R < 0: R = R + Y` | **同号相减，异号相加**：<br>• $R$ 与 $Y$ **同号**：$R = R - Y$，商上 `1`<br>• $R$ 与 $Y$ **异号**：$R = R + Y$，商上 `0` | 将“无符号除法/纯正数除法”的判断条件强行套用在“有符号数”说明中               |
| **错误 3：溢出检测条件**      | `被除数绝对值 >= 除数绝对值 (有符号数)`                      | **整数除法溢出仅有两种**：<br>1. 除数 $Y = 0$<br>2. 最小负数除以 $-1$（如 $-2^{31} \div -1$）                          | **张冠李戴**！把**定点小数除法**的专属溢出条件（商必须 $<1$）错误绑定到整数除法中 |


核心错误深度分析与订正

### 1. 错误一：初始化规则冲突（零扩展 vs 符号扩展）
* **课件问题**：在步骤 1 中写死 `R = 0`，但下方异常检测又注明 `(有符号数)`。
* **正确逻辑**：
  * **无符号整数除法**：被除数存放在 $Q$ 寄存器中，高位余数寄存器 $R$ 需要对被除数做**零扩展**，因此 `R = 0` 正确。
  * **有符号补码整数除法**：被除数为 32 位有符号数时，参与 64 位除法前，$R$ 寄存器必须保存被除数的**符号扩展（Sign Extension）**：
    * 若被除数为正，$R = 00\cdots00B$
    * 若被除数为负，$R = 11\cdots11B$

---

### 2. 错误二：加减交替逻辑错误（单看余数正负 vs 对比双方符号）
* **课件问题**：仅凭余数 $R$ 的正负（`R >= 0` 或 `R < 0`）来决定下一步是加 $Y$ 还是减 $Y$。
* **正确逻辑**：
  * `R >= 0 减 Y`、`R < 0 加 Y` **仅适用于无符号数除法**。
  * 对于**408 核心考点——有符号补码加减交替法**，必须根据余数 $R$ 与除数 $Y$ 的**符号位关系**动态决定：
    $$\text{下一步操作} = \begin{cases} 
    R = R - Y, & \text{当 } \text{Sign}(R) == \text{Sign}(Y) \text{ （同号相减，商上 1）} \\ 
    R = R + Y, & \text{当 } \text{Sign}(R) \neq \text{Sign}(数最大能表示的上限 $+2^{n-1}-1$。

## 408考点总结

- ALU通过MUX和求反实现加减切换
- 乘法器通过**移位**减少位宽（不需要n×n位的乘法器）
- 除法器通过**试商**（减法+判断符号）逐位确定商
- 标志位在ALU输出时同步生成
- 乘法器用**右移**，除法器用**左移**
