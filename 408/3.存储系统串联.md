# 3. 存储系统（计组+OS融合）

> 本章融合计算机组成原理和操作系统中的存储相关内容，按主题组织，强调知识串联。

## 3.1 核心对比表
### 3.1.1 存储层次结构

| 存储层次           | 访问速度         | 容量               | 成本  | 由谁管理  | 对程序员 | 主要技术  |
| -------------- | ------------ | ---------------- | --- | ----- | ---- | ----- |
| **寄存器**        | 最快（<1ns）     | 最小（KB级）          | 最高  | 编译器   | 可见   | 硬件    |
| **Cache**      | 很快（1-10ns）   | 小（MB级）           | 高   | 硬件自动  | 透明   | SRAM  |
| **主存**         | 较快（50-100ns） | 中（GB级）           | 中   | OS+硬件 | 透明   | DRAM  |
| **虚拟内存**       | -            | 由地址位数决定（32位→4GB） | -   | OS    | 透明   | 页表/段表 |
| **辅存（磁盘/SSD）** | 慢（ms级）       | 最大（TB级）          | 最低  | OS+用户 | 可见   | 机械/闪存 |

**重要说明**：
- **虚拟内存容量** = 地址总线位数决定（如32位系统最大4GB，64位系统理论上16EB）
- **实际可用虚拟内存** ≈ 物理内存 + 交换空间（磁盘上的页面文件/swap分区）
- 虚拟内存不是"把磁盘当内存用"，而是一个地址映射机制，给每个进程独立的地址空间

**核心原理**：
- **时间局部性**：最近访问的数据很可能再次访问（利用Cache/页面缓存）
- **空间局部性**：相邻地址的数据很可能一起访问（利用Cache行/页面）
- **层次调度**：数据在各层之间按需调度，对上层透明

**408考点**：
- 各层之间的速度差异（数量级）
- Cache和虚拟内存都利用了局部性原理
- 透明性：Cache对程序员透明，虚拟内存对程序员透明

---

### 3.1.2 地址映射方式对比

#### Cache地址映射（计组）

| 映射方式      | 地址划分      | 硬件复杂度    | 命中率 | 冲突      | 替换算法         |
| --------- | --------- | -------- | --- | ------- | ------------ |
| **直接映射**  | 标记+索引+块内  | 低        | 低   | 多（固定位置） | 不需要          |
| **全相联映射** | 标记+块内     | 高（并行比较器） | 高   | 少       | 需要（LRU/FIFO） |
| **组相联映射** | 标记+组索引+块内 | 中        | 中   | 折中      | 组内需要         |

#### 页式映射（OS）

| 映射方式     | 地址划分         | 页表层级 | 空间开销    | 查找速度   | 适用场景  |
| -------- | ------------ | ---- | ------- | ------ | ----- |
| **单级页表** | 页号+页内偏移      | 1级   | 大（连续空间） | 快      | 小地址空间 |
| **多级页表** | 一级页号+二级页号+偏移 | 2-4级 | 小（按需分配） | 较慢     | 大地址空间 |

#### 映射方式对比（OS）

| 特性 | 页式 | 段式 | 段页式 |
|-----|------|------|--------|
| **划分依据** | 固定大小 | 逻辑单位（可变） | 段内分页 |
| **地址结构** | 页号+页内偏移 | 段号+段内偏移 | 段号+页号+页内偏移 |
| **碎片** | 内部碎片 | 外部碎片 | 内部碎片 |
| **共享/保护** | 较难 | 容易 | 容易 |
| **透明性** | 完全透明 | 对用户可见 | 结合 |

**核心区别**：
- **Cache映射**：物理地址→Cache位置，硬件实现，纳秒级
- **页式映射**：虚拟地址→物理地址，OS管理+硬件辅助（MMU），微秒级（有缺页时毫秒级）
- **目标不同**：Cache为了速度，页表为了扩展地址空间

---

### 3.1.3 替换算法对比

#### Cache替换算法（计组）

| 算法 | 原理 | 硬件实现 | 命中率 | 开销 |
|-----|------|---------|-------|------|
| **随机(RAND)** | 随机选一块 | 简单（随机数生成器） | 低 | 最低 |
| **FIFO** | 先进先出 | 简单（队列计数器） | 中 | 低 |
| **LRU** | 最近最少使用 | 复杂（访问时间记录） | 高 | 高 |
| **LFU** | 最不经常使用 | 复杂（访问次数计数） | 中 | 高 |

#### 页面置换算法（OS）

| 算法 | 原理 | 是否需要硬件支持 | 性能 | Belady异常 | 实现难度 |
|-----|------|---------------|------|----------|---------|
| **最佳(OPT)** | 置换最长时间不用的 | 否（理论算法） | 最优 | 无 | 不可实现 |
| **FIFO** | 先进先出 | 否 | 差 | **有** | 简单 |
| **LRU** | 最近最少使用 | 是（访问位） | 好 | 无 | 困难 |
| **Clock(二次机会)** | FIFO+访问位 | 是（访问位） | 较好 | 无 | 中等 |
| **改进Clock** | Clock+修改位 | 是（访问位+修改位） | 好 | 无 | 中等 |
| **LFU** | 最不经常使用 | 是（计数器） | 较好 | 无 | 困难 |

**Belady异常**：
- 增加页框数，缺页次数反而增加
- 只有FIFO会出现

**408重点**：
- LRU是Cache和页面置换的首选（性能好）
- 实际系统常用Clock算法（LRU的近似，实现简单）
- FIFO简单但性能差，且有Belady异常

---

### 3.1.4 TLB vs Cache

| 对比维度      | TLB（快表）           | Cache       |
| --------- | ----------------- | ----------- |
| **组成**    | Tag/控制位/实页号       | 控制位/Tag/数据块 |
| **作用**    | 缓存页表项（虚拟页号→物理页框号） | 缓存数据和指令     |
| **输入**    | 虚拟页号              | 物理地址        |
| **输出**    | 物理页框号             | 数据          |
| **位置**    | MMU内部             | CPU与主存之间    |
| **大小**    | 很小（几十到几百项）        | 较大（KB到MB）   |
| **映射方式**  | 全相联/组相联           | 直接/组相联/全相联  |
| **未命中代价** | 访问页表（几十ns）        | 访问主存（几十ns）  |
| **管理者**   | OS（页表内容）+硬件（TLB）  | 硬件自动        |

**协同工作**：
```
CPU发出虚拟地址
  ↓
查TLB（虚拟页号→物理页框号）
  ↓
TLB命中 → 得到物理地址 → 查Cache
TLB未命中 → 查页表 → 更新TLB → 查Cache
  ↓
Cache命中 → 返回数据
Cache未命中 → 访问主存 → 更新Cache → 返回数据
```

**408考点**：
- TLB是虚拟内存的加速机制，Cache是主存的加速机制
- 两者都利用局部性原理
- 访问顺序：TLB → 页表 → Cache → 主存

---

## 3.2 计算题模板

### 3.2.1 Cache命中率与访问时间（计组）
- [ ] 这部分挪到计组中，因为是单独部分，跟OS无关
**公式汇总**：
- **平均访问时间** = 命中时间 + 未命中率 × 未命中开销
  - AMAT = Hit_time + Miss_rate × Miss_penalty
- **有效访问时间** = 命中率 × Cache访问时间 + 未命中率 × (Cache访问时间 + 主存访问时间)
  - EAT =  t_c + (1-h) × t_m
- **Cache容量** = 块数 × 块大小
- **主存块数** = 主存容量 ÷ 块大小

**解题步骤**：
1. 【待补充：计算Cache容量和块数】
2. 【待补充：确定映射方式和地址划分】
3. 【待补充：计算命中率】
4. 【待补充：计算访问时间】

**典型例题**：
【待补充】

---

### 3.2.2 虚拟地址转物理地址（OS+计组）

**公式汇总**：
- **虚拟地址** = 页号 × 页面大小 + 页内偏移
- **物理地址** = 页框号 × 页面大小 + 页内偏移
- **页号位数** = log₂(虚拟地址空间 ÷ 页面大小)
- **页内偏移位数** = log₂(页面大小)

**解题步骤**：
1. 【待补充：确定页面大小和地址位数】
2. 【待补充：划分虚拟地址（页号+偏移）】
3. 【待补充：查页表得到页框号】
4. 【待补充：拼接物理地址（页框号+偏移）】

**典型例题**：
【待补充】

---

### 3.2.3 页面置换算法（缺页次数计算）（OS）

**常见算法模板**：
- [ ] 这部分等着挪到OS
#### FIFO模板
```
页面访问序列：1 2 3 4 1 2 5 1 2 3 4 5
物理块数：3

时刻    1  2  3  4  1  2  5  1  2  3  4  5
块1     1  1  1  4  4  4  4  4  4  3  3  3
块2     -  2  2  2  2  2  5  5  5  5  4  4
块3     -  -  3  3  3  3  3  3  3  3  3  5
缺页    √  √  √  √     √     √        √  √  √
```

#### LRU模板
```
页面访问序列：7 0 1 2 0 3 0 4 2 3 0 3
物理块数：3

时刻    7  0  1  2  0  3  0  4  2  3  0  3
块1     7  7  7  2  2  2  2  4  4  4  0  0
块2     -  0  0  0  0  0  0  0  0  3  3  3
块3     -  -  1  1  1  3  3  3  2  2  2  2
缺页    √  √  √  √     √           √     √
```

**解题步骤**：
1. 【待补充：画表格，列出时刻和物理块】
2. 【待补充：逐个处理访问序列】
3. 【待补充：应用置换算法规则】
4. 【待补充：统计缺页次数，计算缺页率】

**典型例题**：
【待补充】

---

### 3.2.4 综合题：TLB+Cache+页表的访问时间（融合）

**公式汇总**：
```
总访问时间 = 
  TLB命中 × (TLB时间 + Cache访问) + 
  TLB未命中 × (TLB时间 + 页表访问 + Cache访问)

其中Cache访问 = 
  Cache命中 × Cache时间 + 
  Cache未命中 × (Cache时间 + 主存时间)
```

**解题步骤**：
1. 【待补充：识别各部分命中率】
2. 【待补充：列出时间参数】
3. 【待补充：分情况计算】
4. 【待补充：加权求和】

**典型例题**：
【待补充】

---

## 3.3 知识串联

### 3.3.1 CPU访问数据的完整路径

**完整流程图**：
```
1. CPU执行指令，产生虚拟地址
   ↓
2. MMU（内存管理单元）接收虚拟地址
   ↓
3. 查TLB（快表）
   ├─ TLB命中 → 得到物理页框号 → 跳到步骤5
   └─ TLB未命中 ↓
   
4. 查页表（在主存中）
   ├─ 页表命中 → 得到物理页框号 → 更新TLB → 继续
   └─ 缺页中断 → OS调入页面 → 更新页表 → 更新TLB → 继续
   
5. 拼接物理地址（物理页框号 + 页内偏移）
   ↓
6. 查Cache
   ├─ Cache命中 → 返回数据 → 结束
   └─ Cache未命中 ↓
   
7. 访问主存
   ↓
8. 主存返回数据 → 更新Cache → 返回CPU
```

**时间开销分析**：
- **最快路径**：TLB命中 + Cache命中 ≈ 几ns
- **中等路径**：TLB未命中 + Cache命中 ≈ 几十ns
- **慢路径**：TLB命中 + Cache未命中 ≈ 几十到上百ns
- **最慢路径**：缺页中断 ≈ 几ms（磁盘I/O）

**408考点**：
- 虚拟地址转换发生在Cache查找之前
- TLB加速地址转换，Cache加速数据访问
- 缺页中断是最大的性能瓶颈

---

### 3.3.2 多级存储的协同工作原理

#### 层次间的数据流动

**向上流动（CPU请求数据）**：
```
辅存 ──页面调入──→ 主存 ──Cache行调入──→ Cache ──取数──→ 寄存器
     (OS管理)          (硬件自动)         (硬件自动)
```

**向下流动（数据写回）**：
```
寄存器 ──存数──→ Cache ──写回/写直达──→ 主存 ──页面换出──→ 辅存
              (硬件控制)                (OS管理)
```

#### 一致性问题

| 问题 | 发生层次 | 解决方案 |
|-----|---------|---------|
| **Cache一致性** | Cache ↔ 主存 | 写直达/写回 + 监听协议（多核） |
| **页面一致性** | 主存 ↔ 辅存 | 修改位（Dirty bit）+ 写回策略 |
| **多级Cache一致性** | L1 ↔ L2 ↔ L3 | 包含性策略/MESI协议 |

**408考点**：
- 写策略：写直达（简单但慢）vs 写回（快但复杂）
- 修改位的作用：避免干净页的写回
- 多核系统的Cache一致性

---

### 3.3.3 局部性原理在各层的应用

#### 时间局部性

| 存储层次 | 利用方式 | 具体机制 |
|---------|---------|---------|
| **Cache** | 最近访问的数据留在Cache | LRU替换算法 |
| **TLB** | 最近翻译的页表项留在TLB | 全相联快速查找 |
| **页面缓存** | 最近访问的页面留在内存 | LRU/Clock置换算法 |

#### 空间局部性

| 存储层次 | 利用方式 | 具体机制 |
|---------|---------|---------|
| **Cache** | 以行（块）为单位调入 | 一次调入整个Cache行（如64字节） |
| **页面** | 以页为单位调入 | 一次调入整个页面（如4KB） |
| **预取** | 预测并提前调入 | 硬件预取器 |

**局部性失效的情况**：
- **时间局部性差**：数据只访问一次（如流式处理）→ Cache命中率低
- **空间局部性差**：访问地址跳跃（如链表遍历）→ 预取失效
- **工作集超出容量**：活跃数据>Cache/内存大小 → 频繁置换

**408考点**：
- 程序优化：提高局部性 → 提高命中率
- 循环顺序：按行/按列遍历数组的性能差异
- 为什么链表比数组慢：空间局部性差

---

## 3.4 个人理解笔记

### 3.4.1 为什么需要多级存储？

**核心矛盾**：速度 vs 容量 vs 成本

**类比理解**：
- **寄存器** = 你的工作台（最快，但很小）
- **Cache** = 你的书桌抽屉（快，小）
- **主存** = 你书房的书架（中速，中等容量）
- **虚拟内存** = 家里所有房间的书架（扩展空间）
- **辅存** = 仓库（慢，但容量巨大）

你不可能把仓库的所有书都放在工作台上，所以需要：
- 把常用的放在抽屉（Cache）
- 把最近要用的放在书架（主存）
- 把不常用的放在仓库（磁盘）

### 3.4.2 虚拟内存的本质

**不是**：把磁盘当内存用（这只是表面现象）

**本质**：给每个程序一个"假象"——它拥有整个地址空间

**三大作用**：
1. **扩展地址空间**：程序可以使用比物理内存更大的地址空间
2. **内存保护**：每个进程有独立地址空间，互不干扰
3. **内存共享**：多个进程可以映射到同一物理页（如共享库）

### 3.4.3 TLB存在的意义

**没有TLB的世界**：
- 每次访问数据都要先访问页表（在主存中）
- 等于每次访问变成了两次访问（一次页表，一次数据）
- 性能损失50%以上

**有了TLB**：
- 页表项缓存在TLB（全相联，速度快）
- TLB命中率通常>95%
- 大部分访问只需一次内存访问

**类比**：
- 页表 = 字典（每次都要查）
- TLB = 你脑子里记住的常用词（快速回忆）

---

## 3.5 真题复盘

### 真题1：
> 2010－二级页表

**我的错选**：  
**正确答案**：  
**坑点分析**：  
**知识盲区**：  
**防错方法**：  

---

### 真题2：
> 2012－虚拟存储器

**我的错选**：  
**正确答案**：  
**坑点分析**：  
**知识盲区**：  
**防错方法**：  

---

### 真题3：【待补充】
> 2025

**我的错选**：  
**正确答案**：  
**坑点分析**：  
**知识盲区**：  
**防错方法**：  

---

## 3.6 待整理内容

- [ ] Cache的写策略详解（写直达、写回、写分配）
- [ ] 多级页表的地址转换详细过程
- [ ] 段页式存储管理
- [ ] 内存分配算法（首次适应、最佳适应、最坏适应）
- [ ] 工作集模型
- [ ] 颠簸（Thrashing）现象
- [ ] 【补充你学习过程中发现的重要知识点】
